const api = '/api/v1/';
const toursRoute = `${api}tours`;
const usersRoute = `${api}users`;
const reviewsRoute = `${api}reviews`;
const specific = '/:id';

module.exports = { api, toursRoute, usersRoute, reviewsRoute, specific };
