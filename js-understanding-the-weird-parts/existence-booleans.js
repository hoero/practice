var a, a = null, a = ''; // False is the Boolean of these

if (a) { // This will attempt to convert to a Boolean whatever is inside the ().
    console.log('Something is here');
}

// Nothing will happen because all those values are false.